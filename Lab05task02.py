# Q2: Why is synchronization important in parallel programming?

# Parallel processes run independently and at different speeds.
# Without synchronization it will cause race conditions, deadlocks, incorrect results, or non-deterministic behavior.
